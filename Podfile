# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ForReels' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pod is a library someone else wrote
  # Pods for ForReels
  pod "AlamofireImage"


  target 'ForReelsTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ForReelsUITests' do
    # Pods for testing
  end

end
